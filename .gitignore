# R package build artifacts
*.Rcheck/
*.tar.gz

# RStudio project files
*.Rproj
.Rproj.user/

# R session files
.Rhistory
.Rdata
.RData
.httr-oauth

# OS files
.DS_Store
Thumbs.db
nul

# Test VCF files (root-level only — inst/extdata/ files must be tracked)
/chr1.vcf
/chr1.vcf.gz
/chr1.vcf.gz.tbi
/chr1.old.vcf
/chr2.vcf
/chr2.vcf.gz
/chr2.vcf.gz.tbi

# VCF summary files (root-level only — inst/extdata/ copy must be tracked)
/chromosome*VCFsummary.rds

# Junk directory
junk/

# Byte-compiled / optimized
*.Rproj.user
