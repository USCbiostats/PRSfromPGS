# Vignette Development Task List

## Completed Tasks
- [x] 1. VCF Preparation section (bgzip, tabix indices, examples)
- [x] 2. Processing VCF Files section (`prepareVCFfiles()` with examples)
- [x] 3. Reading PGS Models section (`readPGSmodel()` with examples)
- [x] 4. REF/ALT allele matching explained inline with `calculatePRS()`
- [x] 5. Calculating PRS for Single Model section (`calculatePRS()` with examples)
- [x] 6. Batch Scoring Multiple Models section (`fitPRSmodels()` with examples)

## Remaining Tasks
- [x] 7. Advanced Topics section (parallel processing, memory, troubleshooting tips)

## Status: COMPLETE

## Notes
- Each section should include working examples using inst/extdata files
- Examples should be executable (eval=TRUE by default)
- Focus on practical use cases
- Keep explanations concise but clear
